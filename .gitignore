.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.example.tfvars
# Env-selector tfvars (no secrets — env name, tags, cost center only)
!environments/**/*.tfvars
.env
*.log
PROGRESS.md
# Terraform plan files (may contain secrets/sensitive values)
tfplan
*.tfplan